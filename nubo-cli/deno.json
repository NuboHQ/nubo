{
  "importMap": "./import_map.json",
  "tasks": {
    "dev": "denon run -A nubo.ts",
    "start": "deno run -A nubo.ts",
    "install": "deno install -qAf nubo.ts"
  },
  "lint": {
    "rules": {
      "exclude": ["no-explicit-any"]
    }
  }
}
