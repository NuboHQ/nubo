---
import Test from '@/components/Test';
import Layout from '../layouts/Layout.astro';
import { client } from '@nubojs/graphql';

const result = await client.query({
  pages: {
    id: true,
    title: true,
  },
  websites: {
    id: true,
    domains: true,
  },
});

console.log(result);
---

<Layout title="Nubo">
  <main>
    <h1 class="py-4 text-2xl font-bold">Nubo</h1>

    <Test client:load pages={[]} />
  </main>
</Layout>
